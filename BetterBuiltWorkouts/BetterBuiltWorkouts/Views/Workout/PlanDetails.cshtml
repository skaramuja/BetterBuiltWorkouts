@model Plan
@{ 
    ViewBag.Title = "Plan Details";
}

<div class="container form-container">

    <p>Name: @Model.Name</p>
    <p>Created By: @Model.CreatedBy</p>
    @foreach (var exercise in Model.Exercises)
    {
        <p>Exercise: @exercise.Name</p>
    }


<form asp-action="PlanDelete" method="post">
    <input type="hidden" asp-for="PlanId" />

    @{
        if (Model.CreatedBy == User.Identity.Name)
        {
            <button type="submit" class="btn btn-primary">Delete</button>
            <a asp-controller="Workout" asp-action="PlanEdit" asp-route-id="@Model.PlanId" class="btn btn-primary">Edit</a>

        }
    }
    <a asp-controller="Workout" asp-action="CopyPlan" asp-route-id="@Model.PlanId" class="btn btn-primary">Copy</a>
    <a asp-controller="Workout" asp-action="PlanList" class="btn btn-primary">Cancel</a>

</form>
</div>